# 数字员工可靠性与资源配置实施计划

基线：33fa31c / 0.2.9。依据本对话已确认的十二节开发与验收方案。
保留现有未跟踪 PDF 复现文件；不将其视为本次端到端验收证据。
仅内部开发跟踪，不创建飞书方案文档。目标完成前不删除未完成项。

## Stage 1: 对话上下文与身份
**Goal**: 显式引用、每轮发言者、群聊静态身份修正、预算与跨会话隔离。
**Success Criteria**: C01-C06；无引用正常消息无额外远程请求；引用读取有界。
**Tests**: 输入转义、缺失/撤回/跨群引用、Thread+群背景、A/B复用、引用自己回复。
**Status**: In Progress

实现进展（2026-09-16）：已增加结构化逐轮身份/引用模块、转义及共享参考预算、同scope缓存定向查询、两种飞书接入的message.get适配；共享群不再固化首位用户和首条触发消息的环境变量。保留显式引用自己的回复；缓存明确标记未核实快照，较新的撤回状态优先。
最终本地回归：160/160通过，npm run check、新模块语法检查、npm run build、git diff --check通过。补充覆盖最终上下文预算裁剪后不得误记完整交付回执。现有check是语法检查，不声称完成TypeScript全量类型检查。
证据：原始143项测试、check与build通过；新增自动化及完整回归记录见本阶段测试结果。真实MA隔离Session `sesn-20260916030636-vf5pc`：引用“需要”回显正确；复用同一Session后当前用户从A切到B正确。飞书输入/输出为模拟，不作为真实Channel端到端验收。
性能观察：两轮MA调用4506ms/32088ms；第二轮MA事件中存在4次0-token模型请求，间隔约5/8/8/8秒，随后有效模型请求约2.5秒。仅证实平台事件有多次空请求及等待，尚不能确认具体上游错误；不宣称性能验收完成。
未完成验收：真实飞书引用/Thread/权限及完整多轮回归留待Stage 5。底层node-sdk不暴露单请求AbortSignal，Gateway等待上限3秒；Channel底层HTTP仍依赖其30秒超时，不宣称请求已物理取消。

## Stage 2: Session 配置与诊断
**Goal**: 声明式 JSON、原生参数保留、身份约束、指纹与版本/doctor、AppID校验、单实例保护。
**Success Criteria**: S01-S05、E01-E02；不自动覆盖Agent，不重建旧Session。
**Tests**: 合并优先级、未知字段、挂载冲突、UAT拒绝、版本命令、旧库迁移、启动锁。
**Status**: Not Started

## Stage 3: 压缩与授权恢复
**Goal**: 持久化压缩检查点与退避；每轮刷新、结构化认证错误、凭证同步、授权等待/取消/恢复。
**Success Criteria**: P01-P05、A01-A07；不串身份、不静默handoff、不盲重放写请求。
**Tests**: 同样本/无新消息/重启防重、并发刷新、失效/网络/权限分类、错账号/重复回调、部分成功。
**Status**: Not Started

## Stage 4: 文件终态与平台扩展
**Goal**: 附件阶段追踪、运行与交付状态区分、早停准确反馈；验证upgrade与动态MemoryStore契约后接入。
**Success Criteria**: F01-F05、U01；未证实接口不伪实现；客户PDF问题未复现不关单。
**Tests**: 双PDF/多页/扫描/混合/损坏/超限、同名/重启/压缩、SSE异常、upgrade互斥/超时/实际生效。
**Status**: Not Started

## Stage 5: 完整回归与真实端到端
**Goal**: 自动化、真实MA、开发机真实飞书、性能对照、迁移与回退演练。
**Success Criteria**: 全部确定性测试通过；安全失败零容忍；真实用户入口证据，不以模拟替代；逐项完成审计。
**Tests**: 个人助手/数字员工单聊/群聊/Thread，排队表情/流式，OAuth/文件；同配置延迟对照。
**Status**: Not Started

## 验收编号与证据登记

- C01-C06：引用理解、窗口外定向获取、撤回/权限、Thread、注入防护、多人身份（待验证）。
- S01-S05：无配置兼容、Memory/TOS挂载、合并资源、身份冲突拒绝、配置变化不重建（待验证）。
- P01-P05：同样本防重、重启、无消息、串行压缩、失败不轮换（待验证）。
- A01-A07：旧Session刷新、失效OAuth、错误分类、多应用隔离、Credential同步、回调、写入不重放（待验证）。
- F01-F05：未@附件、部分失败、提前idle、文件持久、SSE（待验证）。
- E01-E02：AppID与脱敏版本诊断（待验证）。
- U01：upgrade成功/失败/未知与身份边界（待核实接口）。
- 平台依赖：MA官方页面此前读取失败；本轮重新寻找可验证契约。测试额度使用已有授权账号，生产Agent不自动修改。
- 发布：本次开发目标不等于授权推送npm/GitHub或切换生产网关；完成后提供可验收代码与证据，外部发布按明确请求执行。
