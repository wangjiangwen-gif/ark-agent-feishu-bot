# 数字员工可靠性与资源配置实施计划

基线：33fa31c / 0.2.9。依据本对话已确认的十二节开发与验收方案。
保留现有未跟踪 PDF 复现文件；不将其视为本次端到端验收证据。
仅内部开发跟踪，不创建飞书方案文档。目标完成前不删除未完成项。

## 本轮增量：仅手动压缩与运行失败收尾（2026-09-16）

用户变更：取消 Gateway 按 token/事件阈值自动发送 `/compact`，常规压缩交由 MA 自身管理。以下增量不取代 Stage 1–5 的未完成范围，开发机保持 npm 0.2.9，不自动部署。

### Stage A: 仅手动触发压缩
**Goal**: 普通消息不检查阈值、不发送压缩命令；用户 `/compact`（大小写兼容）沿原 Session 串行执行。
**Success Criteria**: 个人助手、数字员工单聊/群聊/Thread 和旧配置均无自动压缩；保留未决旧操作只读核查，原生事件缺失不报成功。
**Tests**: 高 token/大量事件/重启/旧阈值配置、群聊未@、手动命令去重和串行、文件保留、旧未决检查点。
**Status**: Complete

### Stage B: 失败原因和流式卡片收尾
**Goal**: 安全保留 MA 运行错误分类，失败后不留下无限 Thinking 占位。
**Success Criteria**: 不暴露上游正文或凭证、不误记业务成功、不自动重跑写入；正常流式行为不回退。
**Tests**: PDF file_url 超时、无正文/已有正文后失败、卡片收尾失败、非流式/SDK备用路径、分片SSE与轮询。
**Status**: Complete

### Stage C: 回归与剩余真实验收
**Goal**: 全量本地测试、构建与差异检查，核实手动压缩真实契约，继续文件/PDF隔离验证。
**Success Criteria**: 分开记录本地模拟与真实外部结果，不将PDF超时或普通总结冒充成功，不修改用户正在测试的开发机。
**Tests**: 全量自动化；有明确权限和可用契约后使用隔离Session真实验证。
**Status**: In Progress

本轮实现：移除Gateway自动阈值判定和定期统计查询（包括旧配置入口），仅显式手动命令提交`/compact`，兼容大小写；旧未决检查点仍只读核查，不重发。保留原生压缩事件+idle的成功条件；官方事件参考2559583（2026-09-16更新）列出了原生压缩事件，但尚未找到能保证文本命令触发它的公开请求契约，未对这条路径继续盲试。

失败收尾：使用真实`session.error`结构实现有界白名单解析，识别文件处理超时与限流，保留安全Request ID并进入审计专用字段；SSE/轮询/超时恢复/重启核查共用安全结果。原卡片在执行失败时尝试替换为失败提示、关闭流式；SDK吞掉回调异常也不记成功。仅投递失败时不覆盖可能已送达的正文，保留只读恢复。完整回归发现该恢复场景曾被误覆盖，已修正并保持原测试通过。卡片更新本身失败时只能记录未确认，不保证远端占位一定被清除。

验证：798/798本地自动化通过，零失败/取消/跳过（7085.572834ms）；语法check、build、diff检查通过。新增41项覆盖新策略、/new后多轮、7字节SSE、轮询、敏感正文不转发、失败后idle、单聊/群聊/Thread的Get/审计收尾、SDK吞错、卡片关闭失败、投递只读恢复回归。这里的飞书卡片接口是模拟，不是真实飞书验收。

真实MA隔离测试：先用CSV探针`sesn-20260916145350-wwvmp`，MA拒绝文件类型，明确记失败，未当文件持久性通过。随后PDF技能生成并渲染校验虚构API样本，两组分别为2KB×2、约4.5MB×2；`sesn-20260916145729-lnrdx`与`sesn-20260916145843-w4bvj`均三轮通过。每组创建1Session/上传2文件/压缩统计查询0次/`/compact`命令0条；未@文件后@触发上传→bash哈希→read两份PDF回答四项事实→再次哈希一致。只读核验每组2个base64 document返回且无session.error。小文件三轮16833/16479/13370ms，大文件13459/14003/18395ms。使用生产配置的Agent/Environment引用，但隔离新Session不挂生产Vault，不改Agent或开发机；飞书输入/输出为模拟，不是完整Channel端到端。证据见`docs/test-results/manual-compaction-and-failures-2026-09-16.json`。

原PDF问题仍开放：稳定版既有Session的真实错误是`Timeout while processing file_url`且重试耗尽；本轮合成大小PDF都正常，不能宣称客户文件/原上下文已复现或根因已修复。手动原生压缩、真实飞书卡片收尾与Stage 1–5其余未完成项继续保留。目标已恢复active；本轮没有发布npm/GitHub，没有改变开发机npm0.2.9部署。

## 本轮增量：准备完成后的安全恢复（2026-09-16）

### Stage D: 准备完成检查点
**Goal**: 加密保存已经准备好的最终输入、Session 绑定、上下文回执和提示，不把创建 Session 等同于已派发模型。
**Success Criteria**: 准备检查点不可被替换；CAS 领取、完整绑定及同 scope FIFO；OAuth 续跑和命令不能误用。
**Tests**: 加密重开、篡改/旧 owner/旧 revision、不同配置、已派发、授权代次、输入大小边界。
**Status**: Complete

### Stage E: 原任务恢复与失败通知
**Goal**: 进程在输入准备完成后退出，重启核查 Session 空闲后复用完全相同的输入执行；确认送达的失败卡片不重复发文本。
**Success Criteria**: 不再下载/上传/挂载/创建 Session，不重复开发者创建 hook；派发意图落库后的未知结果仍不重发；其他 scope 可继续。
**Tests**: 真实子进程退出、TXT/Markdown/引用原文、FIFO、并发核查、运行中/配置变化、失败卡片更新不确定时兜底。
**Status**: Complete

### Stage F: 回归与边界登记
**Goal**: 全量本地测试、构建、自审与证据记录；开发机保持稳定版。
**Success Criteria**: 不把本增量当作完整准备事务恢复；Session 创建结果未知、ready 前中断、真实飞书/MA验收及 Stage 1–5 仍独立跟踪。
**Tests**: 全量自动化、语法检查、build、diff 检查。
**Status**: Complete

实现与验证：最终输入、Session绑定、上下文回执和提示加密保存；准备Session与派发Session分开，原始派发记录落库后绝不重发。恢复先核查空闲，再按scope队列领取，恢复输入SHA-256一致；挂载/下载/创建资源不重做。每轮凭证维护继续执行；查询和凭证维护期间新增授权暂停或绑定变化时停止。核查与领取不等待整轮模型执行，避免跨群串行。控制台增加需要显式确认的`resume_prepared`动作，原`reconcile`不启动业务。流式失败正文和关闭均确认后不再重复发相同失败文本；未确认或SDK回退仍保留兜底，失败不记业务成功。

本地新增149项，最终完整947/947通过、零失败/取消/跳过（8880.310792ms）；语法check、build、diff检查通过。真实子进程故障注入覆盖ready前/ready后/dispatch后退出、已有Session、历史TXT/引用Markdown/当前PDF保留、同scope FIFO及跨scope并行、事务回滚、Token维护、查询超时和授权暂停竞态。独立审查实际发现并修复了遗漏凭证维护、恢复跨scope串行、新增授权暂停被绕过，以及OAuth续跑重复失败提示。旧测试曾用整段Environment JSON冒充Session ID，已改为独立合法ID，未放宽生产校验。本地测试的MA/飞书外部接口仍是模拟，不冒充完整真实验收。

真实MA探针：子进程创建隔离Session `sesn-20260916153457-f2lpp`，完成输入持久化后以77退出，尚未投递任何消息。首次恢复失败暴露新Session无历史状态事件，`getSessionStats`无法证明idle；保守停止，0次MA.run/消息POST。只读查询当前资源确认idle后，改用严格的`inspectSessionReadiness`（精确GET、Session/Agent匹配、最多5秒/4MiB、不返回敏感配置），新增48项协议测试及6项Gateway拒绝场景。复用同一Session和原SQLite显式恢复成功：4671ms、1次当前状态核查、1次MA.run/消息POST、1次模拟回复，输入持久化/请求/事件三处SHA一致，随机回显正确；0次重新创建/上传/配置构建/群历史读取，完整历史1条user.message、0压缩命令/事件、0工具调用/运行错误。原失败证据保留，未通过反复建Session掩盖问题。证据见`docs/test-results/prepared-recovery-2026-09-16.json`，探针可使用`--live`或针对明确未派发记录的`--resume`运行。

验收边界：真实MA探针不连接飞书、不挂生产Vault，不实际刷新凭证、不含文件；文件准备恢复使用真实本机子进程+模拟外部API验收。当前状态查询是快照，不能保证外部控制台同时改Session或发消息时原子compare-and-dispatch。ready前中断/创建结果未知、部分成功写入续办、upgrade网关闭环、真实飞书与完整性能/资源组合仍按Stage 1–5继续；没有部署或发布，CLI实验队列保持默认关闭。

## 本轮增量：创建结果核查与纯文本交付回执（2026-09-17）

### Stage G: 创建意图与原子确认
**Goal**: 创建 Session 前加密冻结请求与绑定；创建成功后的会话、配置和二进制挂载回执原子保存。
**Success Criteria**: 默认和实验队列均不因本地保存失败重复创建；未决记录不能被新配置或 `/new` 绕过。
**Tests**: 保存故障、重开数据库、CAS、密文、防重与多 scope 隔离。
**Status**: Complete

### Stage H: 未知创建只读核查与原文回执
**Goal**: 使用唯一创建标记和完整有界查询核实原 Session；TXT/Markdown 只有完整送入 MA 才标记交付。
**Success Criteria**: 不自动重发未知创建、不盲重跑旧任务；预算外原文继续保留待恢复。
**Tests**: 查询不完整/多候选/绑定变化；ready 前失败、历史/引用裁剪、恢复预算、准备检查点重启。
**Status**: Complete

### Stage I: 回归与证据
**Goal**: 本地全量测试、check、build、差异检查和隔离真实验证。
**Success Criteria**: 不把创建资源核实当作完整准备事务完成；其余 Stage 1–5 和真实外部验收继续保留，不发布或部署。
**Tests**: 全量自动化；只在已核实接口范围内做独立 MA 测试。
**Status**: Complete

实现：默认和实验队列均先加密保存完整请求、当前消息/身份/配置及初始二进制挂载意图，再POST创建；两项关联tags保留用户原标签，不作为服务端幂等键。确认时在同一SQLite事务保存创建回执、Session映射、配置、压缩基线与mount回执。未知响应不重POST，按Agent/创建时间完整有界查询列表，再验证唯一双tag候选和详情的Environment、Vault、资源及原生请求字段；不完整、非空闲、配置变化或多候选均保留未知。只有结构化400/InvalidParameter可确认拒绝并允许修正后新建。创建异常安全诊断继续保存，未知mount保持pending，不伪装成未创建。

独立审查修复旧isolated模式三个回退：不同消息创建scope拆分以保留并发；相同消息已confirmed但ready前退出会找到原记录并核查复用；旧shared映射不阻止isolated恢复且不被覆盖。核查中绑定变化、错误Session回执、切换模式均不能绕过未决创建。旧开发态isolated范围无法验证时保守冻结，不删除原数据。实验队列ready前只确认已创建资源，原业务与同scope后续仍暂停，不以此冒充完整准备恢复。

纯文本修复：当前TXT/Markdown、历史/引用及压缩恢复原文仅在完整进入最终输入且MA正常返回、无授权等待后记回执；历史/引用预算裁剪、准备/执行失败不标记已提供。ready检查点增加精确inlineDeliveryKeys并兼容旧记录；压缩恢复按文件清待办，256KB预算外原文保留下一轮。旧错误inline挂载标记不再使引用省略原文。

验证：新增加178项，全量1125/1125通过，零失败/取消/跳过（10524.298916ms）；check（语法）、build、diff检查通过。首次全量发现创建诊断未保留及两个测试将不同创建返回同一个假Session ID；前者修复生产逻辑，后者改为独立模拟ID，不放宽配置冲突校验。独立审查真实子进程退出、正常public accept重复事件、实验队列只读恢复、跨群/isolated并行、事务故障、损坏密文/元数据/旧表、状态拒绝、完整分页/限额/取消、原文预算与恢复均覆盖。外部飞书与MA在本地套件中模拟。

真实MA创建恢复两组都首轮通过：`sesn-20260916161253-pbh8z`直接引用Environment；`sesn-20260916161546-ttmsz`由实际buildSessionCreateRequest构建Environment配置并增加非敏感环境覆写。各创建1次后子进程exit77（本地尚未confirm），父进程各GET列表+详情2次，找回原Session并原子确认，重复创建0、模型消息0、附件0、Vault为空；父流程总耗时987/1104ms（包含子进程创建），不当作用户整轮延迟。列表真实终页省略next_page，查询器据此兼容短终页，满页无游标仍保守未知。未更改Agent/Environment资源本身或开发机。证据见`docs/test-results/session-creation-2026-09-17.json`。

仍未完成：ready前完整准备计划（历史/引用/资源/hook）冻结与续办、人工处理未决准备任务、部分写入续办、Session升级网关状态机、Memory/TOS复杂资源组合、真实飞书完整E2E及性能、客户原PDF问题。此增量不取代Stage 1–5，目标继续active；未部署/发布，开发机保持稳定npm0.2.9，CLI实验队列默认关闭。

## Stage 1: 对话上下文与身份
**Goal**: 显式引用、每轮发言者、群聊静态身份修正、预算与跨会话隔离。
**Success Criteria**: C01-C06；无引用正常消息无额外远程请求；引用读取有界。
**Tests**: 输入转义、缺失/撤回/跨群引用、Thread+群背景、A/B复用、引用自己回复。
**Status**: In Progress

实现进展（2026-09-16）：已增加结构化逐轮身份/引用模块、转义及共享参考预算、同scope缓存定向查询、两种飞书接入的message.get适配；共享群不再固化首位用户和首条触发消息的环境变量。保留显式引用自己的回复；缓存明确标记未核实快照，较新的撤回状态优先。
最终本地回归：160/160通过，npm run check、新模块语法检查、npm run build、git diff --check通过。补充覆盖最终上下文预算裁剪后不得误记完整交付回执。现有check是语法检查，不声称完成TypeScript全量类型检查。
证据：原始143项测试、check与build通过；新增自动化及完整回归记录见本阶段测试结果。真实MA隔离Session `sesn-20260916030636-vf5pc`：引用“需要”回显正确；复用同一Session后当前用户从A切到B正确。飞书输入/输出为模拟，不作为真实Channel端到端验收。
性能观察：两轮MA调用4506ms/32088ms；第二轮MA事件中存在4次0-token模型请求，间隔约5/8/8/8秒，随后有效模型请求约2.5秒。仅证实平台事件有多次空请求及等待，尚不能确认具体上游错误；不宣称性能验收完成。
未完成验收：真实飞书引用/Thread/权限及完整多轮回归留待Stage 5。底层node-sdk不暴露单请求AbortSignal，Gateway等待上限3秒；Channel底层HTTP仍依赖其30秒超时，不宣称请求已物理取消。

## Stage 2: Session 配置与诊断
**Goal**: 声明式 JSON、原生参数保留、身份约束、指纹与版本/doctor、AppID校验、单实例保护。
**Success Criteria**: S01-S05、E01-E02；不自动覆盖Agent，不重建旧Session。
**Tests**: 合并优先级、未知字段、挂载冲突、UAT拒绝、版本命令、旧库迁移、启动锁。
**Status**: In Progress

实现进展（2026-09-16）：声明式配置已接入两种CLI模式；支持default/direct/group/thread分层、原生字段保留、显式空对象和数组、环境切换重新加载、hook后必需资源补回与挂载冲突校验。Vault用途声明、已知用户Vault拒绝、AppID和共享群bot-only保护均位于最终请求边界。新Session记录配置/请求指纹、显式SP覆写标记和版本；旧Session只提示配置差异，不重建。
诊断：`--version/-v`、`employee doctor --json --session`、构建Commit/源码Hash；doctor只读MA和数据库，SP只Hash，不输出凭证；同数据库SQLite事务锁拒绝第二个活跃进程，确认原PID已退出可恢复。
本地回归：187/187通过；npm run check（自动发现全部src模块）、build和diff检查通过。新测覆盖配置分层、空对象清空、未知字段、hook换环境及冲突别名、配置变化保留Session、凭证隔离、版本无凭证启动、诊断脱敏/只读、单实例与真实子进程退出恢复。
真实只读核验：构建产物doctor读取 `sesn-20260916030636-vf5pc` 成功，Agent v4、AppID匹配；该隔离测试使用内存DB，doctor正确报告unknown_legacy_or_external，不误报配置已一致。构建包含dirty标记，未冒充已发布版本。
未完成：真实Memory/TOS多资源挂载（S02）、开发机旧库迁移/回退及完整E2E；代码通过不等于这些验收已完成。README标明新能力仅源码开发版、尚未发布npm。

## Stage 3: 压缩与授权恢复
**Goal**: 持久化压缩检查点与退避；每轮刷新、结构化认证错误、凭证同步、授权等待/取消/恢复。
**Success Criteria**: P01-P05、A01-A07；不串身份、不静默handoff、不盲重放写请求。
**Tests**: 同样本/无新消息/重启防重、并发刷新、失效/网络/权限分类、错账号/重复回调、部分成功。
**Status**: In Progress

本轮先推进压缩检查点。真实隔离Session `sesn-20260916030636-vf5pc` 执行 `/compact` 返回 `model_rate_limited_error`（平台重试耗尽），随后仍出现 `session.status_idle`；不能把idle单独作为压缩成功证据。不会重新创建Session规避该错误。

压缩实现（2026-09-16）：新增SQLite持久化检查点、样本ID/业务边界、5分钟冷却、连续两次明确失败暂停自动压缩；新Session建立空基线，旧Session首次观测仅建立基线。同样本不重复消费；未决提交重启后先核查，无法确认终态不发送新业务；已确认idle但缺压缩证据时只暂停压缩，不阻断正常业务。手动压缩遵守同scope队列。纯文本附件在可能已经压缩后仍标记恢复，失败不新建Session。
官方契约已通过浏览器读取 `https://docs.volcengine.com/docs/82379/2559583?lang=zh`（页面更新2026-09-09）：`agent.thread_context_compacted` 是压缩事件；实现要求本轮该事件和正常idle同时存在。多MA线程不将单个子线程事件误报为全Session压缩。当前无多MA线程全局压缩验收结论。
回归：208/208测试通过；syntax check、build、diff检查通过。新增覆盖原生事件解析、旧证明去重、错误后idle、未决提交互斥、重启防重、串行执行、文件挂载记录保留、平台自动压缩基线。证据见 `docs/test-results/session-compaction-2026-09-16.json`。
真实测试连续3次均被模型供应商限流（每次约29.6秒，retry_status=exhausted），达到本项目3次尝试上限，停止该路径重试。下一步先核实测试模型/配额或等待外部限流解除，再用已更新的smoke-resilience脚本验收原生成功事件和真实文件保留；不能把mock成功路径当作真实成功。授权恢复子阶段尚未开发。

授权子阶段开始（2026-09-16）：先测试并实现每轮凭证检查、channel/app/tenant/user绑定隔离、加密的Token轮换待同步状态、单身份刷新互斥和结构化OAuth错误。已有Session不因刷新新建；群聊不读取用户凭证。后续仍需完成授权等待/取消/超时、恢复任务去重和部分写入不重放，不能以本次增量代替A01-A07完整验收。

授权第一增量回归：237/237通过（含旧版208项）；凭证加密、缺失密钥拒绝重建、CAS与跨Store互斥、进程退出恢复、旋转Token先落盘后同步MA、429退避、错账号/租户、已有Session每轮刷新均有自动化覆盖。旧数据迁移增加事务和唯一归属校验，模拟删除失败时整体回滚；同一Vault禁止分配给多个身份。OAuth保留安全错误符号，数字码并存时仍执行slow_down退避。README增加密钥备份与迁移/回退限制，gitignore排除credential-key。该增量未操作生产凭证，未完成真实OAuth端到端，后续授权生命周期和文件/upgrade阶段继续保留。

授权第二增量（本地提交基线138bb10）：恢复检查点持久化，按完整身份/会话/消息键去重，记录原Session；回调原子领取且入队后再次核对Session，/new或替换后的迟到回调不会创建/恢复旧任务。旧Session无用户Vault时只提示显式选择，不再兼容性静默handoff。若MA状态为running/failed/unknown，不提交恢复任务。数字员工旧per-message群模式也强制Bot-only，防止通过旧开关绕过群聊无UAT约束；个人助手入口保持原策略。
回归244/244、check（语法）、build、diff检查通过；新增7项涵盖数据库重开、重复回调、旧Vault元数据缺失、迟到回调、排队中的/new、MA非idle和旧群模式。尚未完成：完整授权等待任务/卡片的重启恢复和取消超时；部分成功任务还在旧process路径中，不声称已经具备安全续跑；这些必须继续开发再做真实端到端。

授权第三增量（2026-09-16，本地基线adbd333）：Device Flow增加AbortSignal、可取消退避、绝对有效期中断，保留取消请求的服务端结果未知语义。数字员工单聊`/auth cancel`走控制路径，不排业务队列，不触发模型；每个异步授权阶段校验当前流程，旧流程完成不得删除替代流程或触发续跑。取消/过期恢复检查点落库；拒绝、超时、身份或同步错误反馈用户。停机先关闭授权处理器，迟到Credential更新不访问已关闭数据库。卡片明确取消不等于撤销飞书授权，不再声称长期授权仅用于本次协作。
验证：新增15项测试（OAuth底层4项、授权生命周期及Gateway集成11项），全量259/259通过；check（语法）、build、diff检查通过。首次普通沙箱运行仅WebUI本地监听EPERM，允许本机监听后全部通过，没有跳过测试。模拟链路串起真实FeishuOAuth客户端、Gateway、Manager、SQLite：发卡→取消→迟到Token，不更新凭证、不新建Session、不重放任务，Get正常清理。证据见`docs/test-results/authorization-lifecycle-2026-09-16.json`。
尚未闭环：这里只持久化取消/过期终态，不是完整活跃Device Flow重启恢复；单聊授权等待时的队列暂停、恢复后的部分写入防重、真实飞书/MA端到端均待继续。原始请求安全续跑不能以本次取消测试代替。

授权第四增量（2026-09-16，本地基线a7a095f）：活跃流程加密持久化，按starting/card_pending/waiting/polling/verifying/sync_pending/ready恢复；明确pending才续轮询，未知交换或卡片结果停止自动重试。已拿到的Token在身份校验前加密保存，校验后凭证与同步阶段原子落盘；取消与全部关联任务终态也是事务。已领取的业务请求重启后不重复派发。过期待同步Token使用轮换后Refresh Token，短期身份校验不因重启延期，关闭后迟到刷新不访问已关闭数据库。启动连接期间消息先缓存，恢复失败关闭Channel，成功后按序放行。
验证：全量281/281通过（新增22项，包含真实子进程退出恢复、事务故障注入、启动顺序、加密与身份隔离）；check（语法）、build、diff检查。普通沙箱仅WebUI回环监听EPERM，允许本机监听后完整通过。未操作生产凭证，不声明真实飞书OAuth端到端完成。证据见`docs/test-results/authorization-restart-2026-09-16.json`。仍需队列暂停、部分写入安全续跑、平台文件/upgrade及真实端到端。

授权第五增量（2026-09-16，本地基线026d8da）：KeyedQueue增加scope暂停与恢复优先任务；授权生命周期回调只暂停对应单聊，重启恢复在接收新消息前建立暂停。授权成功先续原任务，再放行后续消息；取消/拒绝/过期释放暂停。/auth cancel仍不入业务队列；/new控制任务仅在授权暂停/恢复时优先，先取消旧任务后重置；普通场景保持FIFO。旧流程ID不能释放新流程的暂停，群聊不接受用户授权暂停。
验证：新增11项，完整292/292通过（2345.7285ms），check（语法）、build、diff检查通过。覆盖Get收尾、多用户并行、重启后取消/拒绝/过期/new、旧入口/new兼容，以及20scope/500任务顺序和同scope最大并发为1。证据见`docs/test-results/authorization-queue-2026-09-16.json`。不把模拟链路和队列压力测试当作真实飞书端到端或用户端P95证明。仍需部分写入安全续跑、普通排队消息进程退出后持久化恢复、状态查询控制入口及后续平台/文件验收。

授权第六增量（2026-09-16，本地基线583aacb）：新增逐轮工具执行证据，区分事件ID与tool_use_id，关联结果、操作类型和成功资源ID；不保存原始命令、文件正文或Token。授权触发后用本轮历史补核SSE遗漏，查询等待最多2秒，失败不给自动续跑证明。证据按完整身份/消息/Session绑定加密保存；缺少密钥、Session被篡改、未知/MCP/自定义工具、多MA线程或截断记录均不能证明可自动恢复。只读任务用原用户事件和工具调用ID继续，不再重发原始请求、重新下载附件或读取引用。成功写入或结果不确定时阻止自动重放，反馈已有资源ID并保存authorization_recovery_blocked审计。
验证：新增18项，完整310/310通过（3329.554ms）；check（语法）、build、diff检查通过。覆盖SSE漏写入但历史可查、历史查询失败、重复/冲突事件、复合命令、事件乱序、加密重开/篡改/缺密钥、缺证据和已写入保护、原Session只读续跑、普通请求不增加历史查询。原有队列/OAuth测试改为提供明确只读执行夹具，没有放宽生产判断。普通沙箱仅WebUI回环监听EPERM，允许监听后全量通过。证据见docs/test-results/authorization-evidence-2026-09-16.json。
此增量不是A07完整验收：保守阻止不等于自动跳过已完成写入并继续剩余步骤。仍需验证MA工具执行门禁/幂等契约，完成实际部分写入续跑与真实飞书/MA端到端；未发布、未修改生产环境。其余阶段目标保持不变。

授权第七增量（2026-09-16，本地基线a5c2112）：增加数字员工`/auth status`只读控制入口，绕过授权暂停的业务队列；按应用/租户/用户和当前会话查找本地记录，分别显示凭证、授权阶段、最多10条关联任务恢复状态。明确本地就绪不是服务端实时有效性，授权结束不是业务完成；过期检查不延长期限、不修改流程，查询不刷新Token、不发卡、不调用MA、不重放任务。群聊/Thread只返回Bot-only说明，不读取个人凭证；未配置回调的个人助手路由不变。错误输出脱敏，原始消息、Token、链接和工具输入不进入状态回复。
验证：新增27项测试，完整337/337通过（2852.388708ms），无失败/跳过；check（语法）、build、diff检查通过。覆盖12种流程阶段、6种凭证状态、超期、数据库重开、身份与会话隔离、受阻/领取中任务、输出上限、暂停队列查询、消息去重、群聊和个人助手回归。普通沙箱仅WebUI监听EPERM，允许本机监听后完整通过。证据见`docs/test-results/authorization-status-2026-09-16.json`。没有操作真实飞书/MA、没有部署或发布；本地测试不代替真实端到端及性能验收。仍需普通业务队列持久化、部分写入受控续跑、文件与平台扩展、完整真实端到端，阶段状态保持In Progress。

业务队列持久化第一增量（2026-09-16，本地基线357945e）：新增`MessageInbox`和SQLite接收日志，完整输入（含引用/附件标识）加密，消息ID去重，保存到达顺序、身份范围、Agent和配置指纹。领取前校验当前绑定；同scope强制FIFO，正在准备/已派发/结果未知/等待授权的任务阻止后续领取，不同scope可独立推进。准备阶段也可能创建Session或上传文件，因此进程退出或失败后不能把它当作安全未派发任务重试；记录原阶段及已知Session/请求指纹供后续核查。运行锁、版本CAS、元数据认证和整批恢复事务防止双领、篡改及部分恢复；仅排队数据存在时同样禁止重建丢失密钥。
验证：新增15项，完整352/352通过（2607.417875ms）、check（语法）、build、diff检查通过。包含真实子进程退出、数据库重开、加密/缺密钥/篡改、损坏回滚、顺序与scope互斥、配置变化拒绝、完整消息快照。证据见`docs/test-results/message-inbox-2026-09-16.json`。
明确边界：这是接收日志与恢复分类的基础实现，尚未接入`Gateway.accept`、启动调度或`processed_events`原子接收事务；现有网关普通队列目前仍在内存，不能宣称用户消息重启恢复已经完成。下一增量需接通原子接收、授权暂停、未知任务查询/阻塞解除、控制命令、表情及回复恢复，并以真实Gateway进程崩溃用例验证；不能仅将所有未知任务永久阻塞作为最终方案。其余A07、文件、平台和真实端到端目标继续保留。

业务队列持久化第二增量（2026-09-16，本地基线657df84）：Store新增原子接收、派发、结束、重启恢复入口，去重记录与加密Inbox在同一SQLite事务提交。Inbox使用可嵌套savepoint，提交内层不提前提交外层；更新任一日志失败均回滚。业务消息ID去重先验证原身份，迟到投递不得覆盖原输入或重置领取次数；旧claimEvent入口不能领取已有Inbox任务。旧版事件缺少执行证据，即使failed或超期也不自动迁移成可重放任务。重启分类与事件状态一起更新，损坏或不一致时整批恢复回滚，应用间隔离。
验证：新增16项，针对性31/31通过（879.031333ms），全量368/368通过（2482.587083ms），零失败/跳过；check（语法）、build、diff检查通过。真实子进程分别在外层接收事务提交前与提交后退出：前者两份日志均无残留、可以重新接收，后者两份日志及顺序保留、重复投递不重领。SQLite触发器故障注入覆盖接收、派发、结束及恢复的一致性。初次普通沙箱仅WebUI回环绑定EPERM，允许本机监听后完整回归通过。
边界仍明确：这些入口尚未接入Gateway业务调度与CLI启动，不能宣称线上普通消息重启恢复已完成；下一步应使用这些事务入口接通调度、授权暂停/恢复、未知结果核查、回复与表情恢复。没有操作真实飞书/MA、没有部署或发布；本轮数据库崩溃测试不代替真实端到端。证据见`docs/test-results/message-receive-2026-09-16.json`。其余完整目标与阶段状态不变。

业务队列持久化第三增量（2026-09-16，本地基线db8edc7）：Gateway增加显式`durableQueue`开发选项，接收时原子落库再调度，领取前校验Agent/配置/scope，派发前记录原Session和完整输入Hash；正常结束与授权等待分别记录。恢复入口按到达顺序重新调度queued，重复恢复不重复入队；未知任务仅暂停对应scope，授权终态不能误解锁该暂停。群聊FIFO、Thread/单聊独立、OnIt→Get、流式卡片收尾均复用现有链路。单聊授权完成后沿原持久化任务续跑，只读证据、Session和Vault验证不变；取消/过期事务性终结等待，/new保持原授权控制优先语义。新增授权状态迁移与事件检查点原子性校验，阻止旧resume/handoff入口绕过持久化队列。
验证：新增20项，全量388/388通过（2912.3055ms），零失败/跳过；check（语法）、build、diff检查通过。针对性测试先8项失败→通过，复查新增MA状态running/failed/查询失败三项先失败→修复后通过。真实Gateway子进程分别在排队和MA派发阶段退出：前者恢复原消息顺序、重复投递不重放；后者原Session及未知状态保留、同scope后续仍queued、其他scope可执行。实测的Gateway/SQLite/OAuth Manager为真实实现，MA及飞书传输为模拟，不称真实外部端到端。流式用例证明卡片收尾未完成不会放行后续任务；未证明飞书实际首包延迟改善。
当前边界：CLI默认仍未启用此开发选项，没有切换用户当前运行链路。恢复未知任务目前保守暂停，尚需实现只读核查、解除阻塞/显式处理、准备阶段资源写入核实、授权恢复跨进程衔接、回复/表情投递恢复和CLI默认接入；永久阻塞不是最终完成标准。后续应接通上述链路再做真实飞书/MA与性能验收，不把已通过单元测试当作整体交付。证据见`docs/test-results/durable-gateway-2026-09-16.json`；完整阶段及平台/文件目标保持未完成。

原运行只读核查增量（2026-09-16，本地基线5793e32）：ArkClient增加inspectRun，以持久化的完整输入SHA256定位唯一user.message，返回running、ended或unknown；ended仍只表示运行结束，不代表业务成功。核查读取完整有界历史，拒绝旧idle、同内容多次请求、后续另一轮、冲突事件ID、多MA线程及可见时间倒序。授权错误继续关联原工具调用证据。严格模式验证分页信封/游标/事件身份，设10秒、100页、20000事件、64MiB上限；中断或超限取消响应体，非法UTF-8拒绝解析。旧run/SSE默认路径不切换，核查不POST、不发卡、不执行工具。
验证：先新增27项测试全部失败，再实现通过；追加上限、超时、取消、分片编码等共33项。全量421/421通过（2408.127042ms），零失败/跳过；check（语法）、build、diff检查通过。普通沙箱首次仅WebUI监听EPERM，允许回环后全量通过。真实MA只读检查隔离Session `sesn-20260916030636-vf5pc` 74条事件，最近用户输入重复，返回unknown/ambiguous_anchor，87ms；未提交模型请求或改动资源，不作为完整恢复E2E证明。证据见`docs/test-results/run-inspection-2026-09-16.json`。
未完成边界：inspectRun尚未接入Gateway未知任务调度/解除暂停；还需持久化核查结果、回复投递核查、授权续跑衔接、准备阶段资源核实、CLI启动接入和真实崩溃恢复E2E。不能把只读解析器通过等同于重启恢复完成，其余十二节目标保持未完成。

原运行核查接入增量（2026-09-16，本地基线aa5f011）：持久化Gateway恢复入口接通inspectRun，按批串行查询避免启动查询风暴，按任务合并并发核查。查询前后验证原Session、存储Agent和当前配置绑定；核查结果加密保存并CAS保护，结果未知不解锁。正常回复/流式收尾成功后记录对应RunResult指纹；只有新鲜终态核查、相同结果回复确认、无授权需求同时成立才事务性结束原任务，并在无其他阻塞/授权暂停时恢复scope。原输入密文兼容读取并在状态更新时升级，旧数据不补造送达证明；OAuth再次派发清除旧回复指纹。CLI默认未启用实验队列。
复查修复：原生CardKit content/settings原先未验证HTTP成功响应内的业务code，可能误记回复完成；现要求code=0，否则抛脱敏错误，Gateway不记录送达确认。依据本机node-sdk返回信封与HTTP拦截器源码核实，未猜测新增API。
验证：首批10项恢复测试先失败后通过，另4项CardKit错误测试先失败后修复；共新增23项，全量444/444通过（2882.463417ms），零失败/跳过，check（语法）、build、diff检查通过。真实子进程在确认回复后退出，重开Gateway+SQLite经ArkClient GET历史定位原请求，仅执行下一条消息。覆盖原结果指纹不符、并发重复核查、数据库事务回滚、查询中换Session、存储Agent变化、旧密文迁移、授权暂停不误解锁、CardKit业务错误不误记确认。MA/飞书传输模拟，不称外部E2E；未部署/发布。证据见`docs/test-results/message-reconciliation-2026-09-16.json`。
未完成：缺失回复确认时的消息ID/卡片投递核查与补偿、残留表情清理、准备阶段资源副作用核实、待授权运行的恢复衔接、可面向用户/管理员的明确处理入口、CLI默认接入及真实外部E2E/性能。自动释放仅覆盖已有明确回复确认的路径，不等于完整恢复验收；其余阶段继续保留。

表情恢复增量（2026-09-16，本地基线e3bcaa3）：新增独立加密ReactionStateStore，先记录发送意图再请求飞书，获得ID后记录active，清理前记录removing；失败不删除检查点。表情状态不修改Inbox版本或任务终态，避免干扰原运行核查。Gateway恢复入口清理旧owner和已结束/受阻任务的已知表情，按ID合并并发清理；重复添加同一任务同类未决表情被拒绝，避免旧删除误删平台复用ID的新表情。没有ID的creating不猜测清理，不重跑业务。原生Channel SDK删除表情仅返回void、不验证业务code，适配层改为使用其公开rawClient的原接口并验证code=0，成功字段和路径未猜测。
验证：先5项失败→实现通过，再4项防重复/原生业务错误测试失败→修复通过，最终共新增14项（9项持久化/Gateway恢复、5项适配层），全量458/458通过（2427.782542ms），零失败/跳过；语法check、build、diff通过。真实Gateway子进程在Get确认落库后退出，重启清理原Get/OnIt、不创建Session、不重放原任务，原未知任务和后续queued状态保留。另测密文/身份篡改、缺运行锁、CAS、SQL故障、错误脱敏、完成后清理失败重试和跨应用过滤。外部MA/飞书模拟；普通沙箱首次仅WebUI回环监听EPERM，允许本机监听后完整通过。
边界：CLI仍未默认启用实验队列；未取得表情ID的发送结果、删除响应丢失后的实际状态查询、回复/卡片投递核查、准备阶段资源副作用、授权恢复衔接、人工处理入口及真实外部E2E仍待开发/验收，不将本次已知ID清理视为完整恢复。没有发布、部署或修改真实平台资源。证据见`docs/test-results/reaction-recovery-2026-09-16.json`。

表情结果核查增量（2026-09-16，本地基线256a82a）：已读取lark-cli im.reactions.list schema、技能完整参考及本机node-sdk签名，确认operator_type=app时operator_id是AppID，不能只判断app类型。新增ChannelInspectReaction和飞书完整分页查询（每页50，最多20页，等待5秒），校验业务码、字段、重复ID冲突、游标循环与应用归属；不完整查询不证明不存在。未知发送仅唯一匹配本应用同类表情且action_time在createdAt后30秒内才接管，缺时间/时钟偏差/多匹配保留未知。Gateway按检查点合并核查和清理；已知ID缺失时保存新鲜absent证明并CAS结束清理，避免重复DELETE；无ID空列表继续保留。未决queued旧owner不被无结果查询接管，避免下一次恢复找不到它。CLI适配层接线但durableQueue仍未默认开启，正常交互无额外GET。
验证：新增22项，全量480/480通过（2539.933875ms），零失败/跳过，check（语法）、build、diff通过。新查询测试先模块缺失失败，恢复接入6项先失败后实现通过。覆盖完整分页、用户/其他Bot、时间窗口、未知/缺字段、取消、CAS、并发核查合并和原生适配器→Gateway→SQLite链路“发送响应丢失→找到ID→删除响应丢失→查询不存在”仅一次DELETE。真实只读读取当前配置AppID的已有审计消息，Get/OnIt均code=0、items=[]、has_more=false，分别594ms/415ms；只证明空列表响应兼容，未新增/删除表情、未发消息、未调用MA，不作为完整真实恢复E2E。网页工具无法解析官方markdown，字段依据CLI官方schema与安装SDK，本次未声称网页正文读取成功。
边界：无ID且始终无匹配仍需明确处理入口；缺时间旧数据不自动接管；SDK查询物理取消尚不支持；真实有表情/响应丢失恢复、消息卡片投递核查、其余平台能力与性能验收继续未完成。证据见`docs/test-results/reaction-inspection-2026-09-16.json`。

回复投递检查点增量（2026-09-16，本地基线2937dcc）：新增逐次调用ReplyDeliveryObserver与加密Inbox投递状态。原生卡片创建/发送/正文更新/流式关闭在外部操作前记录意图、成功后记录确认；保存cardId、elementId、messageIds、sequence、正文与运行结果指纹，不把onSent占位卡片回调当最终回复。最终正文与MA结果关联并确认后即可持久化replyConfirmed，覆盖适配层已成功但Gateway尚未完成的进程退出窗口。非流式回复保留全部chunkIds；CLI两种模式和legacy接线，不改变durableQueue默认关闭。每次派发生成独立dispatchId，异步回调和最终确认绑定该次派发，授权恢复即使复用相同Session/输入也拒绝旧回调。
验证：先新增目标用例7项失败，实现后扩展至16项；全量496/496通过（2486.772542ms），无失败/跳过，check（语法）、build、diff通过。真实Gateway子进程在原生卡片最终检查点保存后、Gateway confirm之前立即exit(77)，重启读取dispatched记录、一次只读MA核查后结束任务，不创建Session、不运行MA、不发送回复。另覆盖正文/收尾业务错误、占位/授权、正文不一致、异步保存屏障、重复投递/序号、DB和WAL无明文、授权派发隔离、SDK回退不造最终确认。外部CardKit/MA模拟，未作为真实飞书E2E；普通沙箱全量仅WebUI回环监听EPERM，允许本机监听后完整通过。
性能观察：真实SQLite与模拟CardKit做30组交替对照，1800字符/10次正文更新，每轮13次模拟外部写入，启用检查点不增加外部写请求。无检查点P50/P95=14.260/14.964ms，有检查点=17.692/21.268ms，同步检查点自身P50/P95=3.371/6.546ms。1ms模拟渲染放大本地开销，P95约增加6.3ms（约42%），不能当默认80ms渲染或真实用户P95，不能宣称性能门槛已完成。脚本scripts/probe-reply-checkpoints.mjs可重跑。
发现及边界：安装的Channel SDK 0.4.1流式completeTerminal吞掉部分更新/finishStreamingCard错误且仅返回首条messageId，因此SDK流式回退只记录已知ID，实验队列不接受其最终确认；无observer保持原行为。默认原生CardKit路径严格验证content/settings业务码。接口已生效但响应/本地确认丢失后的远程消息/卡片核查、准备阶段副作用、用户处理入口、CLI默认接入、真实外部E2E和性能继续未完成。没有发布或部署。证据见docs/test-results/reply-delivery-2026-09-16.json。

原生卡片远程投递核查增量（2026-09-16，本地基线7950cfd）：依据飞书官方消息GET/列表markdown规范，新增ChannelInspectReply和原生单卡片查询。只使用已持久化messageId与elementId、已尝试提交的最终正文指纹；GET请求显式选择user_card_content。严格验证业务code、单条响应、应用/租户/chat/thread、未撤回、schema2.0单markdown元素、正文SHA-256一致及streaming_mode=false。缺字段、扁平内容、权限/网络失败、取消、超限响应、多个元素均不造最终确认。Gateway只在原MA运行结束后查询，等待上限5秒；保存前再次校验Session绑定及Inbox revision/dispatchId，证明必须晚于本轮运行核查且30秒内有效。成功保存加密proof后可结束原任务，不重跑MA、不写飞书，正常回复路径不增加查询。CLI接线但实验队列仍未默认开启。
验证：先增加缺失模块/导出的失败测试，再实现；新增29项，全量525/525通过（3286.53875ms），零失败/跳过，check（语法）、build、diff通过。含实际Node子进程在模拟CardKit settings服务端已生效但未返回时exit(78)，重启从finalizing读取检查点，经一次只读消息GET确认并结束，零MA重放/零额外飞书写入。另测content响应丢失、settings响应丢失、并发核查去重、权限失败后重查、迟到结果遇到revision/Session变化、错误身份/群/话题/正文、streaming占位、过期及早于运行核查的证明、加密证明重开可读。外部MA/CardKit故障为模拟。普通沙箱全量仅WebUI回环EPERM，允许本机监听后完整通过。
真实只读证据：现有Bot群消息列表934.5ms、单条GET616.3ms，已更新卡片返回schema2.0、唯一markdown元素683字符、streaming_mode=false，列表与单条正文哈希一致；新增inspectLarkReply直接访问同一消息返回confirmed（695.4ms）。仅证明已更新卡片读取契约与实现兼容，没有新增/修改消息，没有真实故障注入，不作为完整外部E2E或P95性能验收。证据docs/test-results/reply-inspection-2026-09-16.json。
未完成：无messageId的发送结果、普通/分片消息、SDK fallback、未关闭流式卡片的受控收尾、准备阶段副作用、人工处理入口、CLI默认启用与真实端到端/性能验收。没有发布或部署；其余Stage保持原范围。

管理员处理入口增量（2026-09-16，本地基线437f83b）：新增当前应用/Agent范围内的有界任务列表和重新核查/显式放弃接口，在WebUI行为日志内展示。列表只读本地、最多100条，不返回用户原文、模型输出或凭证；操作要求管理Token、同源JSON、任务revision与放弃确认。放弃前重新inspectRun（10秒等待并取消请求），校验原Session/Agent/配置、原请求和原生终态事件、无授权等待；任务失败、接收记录、管理员审计同事务，保持去重，不造送达证明。并发核查互斥；迟到、过期和无证据结果不解除暂停。CLI接线但durableQueue仍默认关闭。
验证：先新增10项失败测试，再实现至通过；最终新增16项，全量541/541通过（3771.297666ms）、零失败/跳过，check（语法）、build、diff通过。真实HTTP覆盖401/403/400/409、错应用、同源确认、分页、脱敏；真实子进程exit(79)后重开验证放弃终态、审计、去重；原子审计失败回滚、超时迟到、缺终态、Session变化等均覆盖。隔离预览服务已停止，未占用生产网关。
边界保持：此入口只处理已确认结束的派发任务，不重跑任务或撤销业务写入；准备阶段、无锚点、仍在执行、授权恢复、无ID发送自动恢复等尚未闭环。浏览器隔离首页加载已验证，按钮路径已通过真实HTTP测试，浏览器控件操作接口未返回文档，未冒称完成实际点击验收。外部MA仍模拟，未部署或发布。测试证据见docs/test-results/queue-control-2026-09-16.json。

## Stage 4: 文件终态与平台扩展
**Goal**: 附件阶段追踪、运行与交付状态区分、早停准确反馈；验证upgrade与动态MemoryStore契约后接入。
**Success Criteria**: F01-F05、U01；未证实接口不伪实现；客户PDF问题未复现不关单。
**Tests**: 双PDF/多页/扫描/混合/损坏/超限、同名/重启/压缩、SSE异常、upgrade互斥/超时/实际生效。
**Status**: In Progress

附件准备证据第一增量（2026-09-16，本地基线b86e581）：新增逐文件SQLite阶段回执，覆盖download/upload/mount/inline/cache，记录起止时间、耗时、实际字节Hash和已确认File/Session ID。初始资源随Session创建和历史/已有Session追加挂载均接入；纯文本不伪装成文件系统挂载。pending只表示未收到结束记录，error也不证明远端没有写入，阶段成功不等于文件已被模型理解。上传回执成功但缓存保存前进程退出时，后续同来源附件可恢复原File ID，不重复下载/上传。新阶段记录不保存正文、Base64或原始错误；同时修复原附件错误文本直接进入模型、飞书回复和普通日志的问题，保留安全的格式/大小/UTF-8提示。

验证：新增12项测试，完整553/553通过（3833.80575ms），零失败/跳过；check（语法）、build、diff检查通过。包含两个真实Gateway子进程退出点（上传请求中、上传已确认但缓存尚未保存）、SQLite重开、分页和scope隔离、多附件部分失败、缓存复用及纯文本阶段。普通沙箱下3项HTTP测试因127.0.0.1监听EPERM失败，允许本机监听后完整通过。每类30次本地SQLite+模拟传输对照：普通文本P95 2.537→2.033ms；两份4.5MiB数据P95 3.451→8.536ms，新增约5.085ms。附件准备相对增幅超过10%，已复审：新增内容Hash和持久化是所需证据成本，尚不声明性能总体验收通过；外部调用次数保持一致，未增加普通轮次查询。证据见`docs/test-results/attachment-stages-2026-09-16.json`，基准脚本`benchmark-attachment-stages.mjs`可复跑。

未闭环：当前是准备阶段证据和一个已确认上传恢复窗口，不是完整准备事务恢复；未知上传/挂载的远端核查、结构化request ID/错误码、工具读取/业务结果关联、管理员可视化查询仍待补充。尚未复现并关闭客户PDF提前idle问题，没有把4.5MiB模拟数据当成真实PDF解析或真实飞书/MA端到端证据。upgrade、Memory/TOS真实访问及Stage 5继续未完成，未发布或部署。

附件管理员诊断增量（2026-09-16，本地基线def1f88）：控制台行为日志新增只读附件阶段查询，不依赖durableQueue。服务端固定channel/AppID范围，明确这是应用历史而非当前Agent专属记录；按源消息与已记录Session精确筛选，序号倒序/游标分页100条，返回租户/群/话题/源消息、阶段结果、耗时、字节Hash和资源ID，不返回消息原文、文件正文和原始错误。只读接口使用既有管理Token，拒绝写入、未知/重复/超长筛选及非法游标；DB错误统一脱敏。页面使用textContent渲染，忽略迟到查询响应，改筛选或读取失败时清空旧结果，阶段成功不标作模型理解。

验证：新增13项（初始9项失败后实现通过，追加3项实际页面脚本与1项查询计划测试），全量566/566通过、零失败/跳过、3816.833625ms；check（语法）、build和diff检查通过。真实HTTP覆盖401/404/405/400/500、队列关闭仍可读、旧库索引迁移、只读数据不变、分页中新写入、SQL参数绑定；页面脚本覆盖XSS文本渲染/竞态/员工切换。隔离浏览器实际点击行为日志、100+5翻页、源消息筛选5条、再叠加Session筛选1条、无匹配空态及截图检查均通过，预览进程已关闭，没有使用生产数据。

性能复查：首版只有应用索引，30,000条合成记录中未命中消息仍需扫描。补上消息/Session索引并验证EXPLAIN使用后，30组交替SQLite对照：无新增诊断索引→有索引，消息未命中P95 20.587→0.038ms，Session未命中22.556→0.032ms，首页0.843→0.466ms；阶段开始/结束写入对P95 0.410→0.526ms（新增0.116ms）。仅本地合成元数据，不是外部端到端性能结论。证据见docs/test-results/attachment-diagnostics-2026-09-16.json，脚本scripts/benchmark-attachment-diagnostics.mjs。

边界：本次补齐管理员可视化，但未知上传/挂载核查、结构化上游错误、PDF工具读取与业务结果关联、upgrade/Memory/TOS和真实完整端到端仍未闭环；不自动部署、不发布、不启用实验队列。遵守MA技能的终态/业务结果分离，未新增或猜测平台API契约。

已有Session附件挂载核查增量（2026-09-16，本地基线f6dc0eb）：上一轮仅汇总状态，无开发进展；本轮继续文件恢复。通过官方添加资源/资源列表文档（2555948、2555950，2026-09-11更新）核对GET列表data数组、最多1000条无分页、返回沙箱挂载路径。新增有界只读inspectFileMount（10秒、4MiB），核对唯一资源ID、File ID和网关实际提示的沙箱路径；不接受未经验证的路径别名，不以空列表证明可以重发。已有Session当前附件与历史附件共用恢复入口：已确认挂载仅补本地标记；未知结果先核查，成功后原子追加mount_check证据并保留原回执。核查绑定原来源/Session/文件/路径，检查时效、Session切换和回执CAS；记录落盘失败整体回滚。不新增Session、不重复POST；仅HTTP400且结构化InvalidParameter拒绝保留后续安全重试。原有两个“泛化网络错误直接重挂载”测试改为明确拒绝夹具，另补未知错误不得重挂载用例，并未放宽未知结果判断。

验证：首批13项测试全部失败后实现，最终新增28项，全量594/594通过、零失败/跳过、4192.077875ms；check（语法）、build、diff检查通过。实际Node子进程分别在模拟远端已生效/尚未返回、挂载成功回执已保存/标记未保存时退出，重开Gateway+SQLite：前者仅一次GET、后者无GET，均不重传、不重挂载、不创建替代Session；后续新用户请求正常执行。覆盖单聊当前附件、群历史附件、错误权限/超时/超限/字段/文件/路径/重复资源、过期与错Session证明、查询中切换Session、无核查适配器、作用域隔离、SQL故障回滚及索引计划。查询使用专用部分索引；正常已有Session追加两份4.5MiB数据，30组交替本地对照（5组预热）：旧直接挂载P50/P95=5.658/9.145ms，新路径=5.695/7.265ms，均70次下载/上传/挂载、35次run、0次额外核查；样本有噪声，不宣称优化了真实链路。证据见docs/test-results/mount-recovery-2026-09-16.json。

边界：尚未取得本次私有Session只读核验授权，没有绕过先前安全审核；外部MA和飞书仍模拟，不称真实E2E。恢复发生在再次处理同一来源附件时，不是启动时完整准备事务恢复；创建Session未获ID、上传未获File ID、结构化request ID、PDF读取/业务结果关联仍待补齐。未部署、发布或启用实验队列；Stage 1–5完整目标保持不变。

新上传响应丢失恢复增量（2026-09-16，本地基线fb53f29）：上一轮挂载恢复已完成增量，本轮继续未知上传。完整读取官方Files上传/列表/详情（1870405/1870407/1870406，2026-09-09更新），未发现服务端内容Hash或幂等键，不以原文件名+大小认领旧文件。新上传先以事务保存UUID操作名/本地字节Hash，再将随机操作名用作multipart文件名；Gateway挂载和提示保留原名，默认ArkClient调用不变。未知结果再次处理时先有界GET列表（20页、2,000条、4MiB、10秒），完整查询要求唯一操作名，再GET详情验证ID、大小、user_data、创建时间窗口（开始前30秒至后120秒）以及active且未过期。追加upload_check并保留原pending/error回执；证明和缓存分离，缓存保存失败也不重查/重传。上传认领按现有缓存源键跨群背景/Thread合并，应用/租户/群/源消息隔离；事务防止并发重复认领，迟到核查不得覆盖已确认的不同File ID。旧无标识记录、扫描不完整或无法确认时不自动重复上传；明确400/InvalidParameter仍可新尝试，新的尝试使用新的操作名。

验证：初始12项全部失败后实现，最终新增30项，全量624/624通过、零失败/跳过、4335.692625ms；check（语法）、build、diff通过。实际Node子进程在模拟远端接收上传后exit(79)，重开Gateway/SQLite，经列表和详情两次GET恢复原File ID，0重复下载/上传，挂载路径保持原中文文件名，原Session继续；外部MA/飞书模拟。补测重复/无标记/错大小/用途/时间/状态/过期、分页循环/空页/错误/条数/总字节限制、中途超时取消、CAS与事务回滚、跨Thread同源合并和跨身份隔离、缓存写失败、明确拒绝和泛化400区分、迟到证明。

性能证据：使用git archive导出的fb53f29源码与当前源码，在同一进程交替执行真实SQLite+模拟传输。最初30组普通文本P95 0.532→0.730ms、双4.5MiB数据7.668→7.701ms；扩大到200组后附件7.310→8.937ms（增加约22%），因此没有直接宣称通过。复查删除正常路径重复读取相同上传记录、把新回执查询改为主键查询，最终200组文本P95 0.342→0.356ms、附件7.380→7.478ms；附件P50 5.995→6.286ms。两组外部调用数一致，各410次下载/上传/挂载/run、0次核查。保留全部三次测量，不把单次差异归因为已证实真实性能提升。脚本benchmark-upload-recovery.mjs，证据docs/test-results/upload-recovery-2026-09-16.json。

边界：随机操作标识是客户端关联策略，不是官方幂等保证或远端内容Hash。旧无标记、扫描超过上限、先写意图但尚未发请求即退出仍保留未知，明确人工重传入口尚未完成；账户文件列表不是快照的竞态仍需真实验收。已询问是否允许在825新建合成测试文件/Session并运行模型，尚未收到答复；没有访问此前被拒绝的私有Session，没有创建真实资源、修改Agent、部署或发布。完整准备事务/CLI队列启用、PDF业务结果关联、Memory/TOS/upgrade及全部真实E2E继续保留原范围。

附件安全错误诊断增量（2026-09-16，本地基线9bd6ef0）：上一轮仅汇总进度，没有代码进展；本轮读取官方错误码页1299023（2026-09-14更新），确认MA HTTP和SSE错误语义不能混用。移除通用MA HTTP请求把上游正文前300字/网络cause直接拼入异常的行为，HTTP保留状态分类、经过格式与已知API Key检查的code及x-request-id；网络错误只保留固定分类和允许的错误码。错误正文限制64KiB/最多5秒，超限/超时/非法UTF-8取消响应流，不把部分JSON当成InvalidParameter拒绝证明。保留JSON及旧纯文本“file type not supported”的固定提示，不复制文件名或正文。该增量不新增重试、不改变明确400/InvalidParameter之外的未知写入判断。

上传、已有Session挂载、创建Session初始挂载以及通用附件阶段接入failure字段，SQLite读取和写入均只保留白名单诊断字段；控制台继续使用textContent，显示分类/HTTP状态/错误码/request ID。未经类型确认的未知错误不能伪造平台拒绝证明。验证：初始13项中9项失败、4项因旧无界读取未结束而取消；实现后补齐UTF-8取消与纯文本兼容回归，新增24项，最终全量648/648通过、零失败/取消/跳过，耗时4454.821625ms，check（语法）、build、diff检查通过。包括真实Gateway与SQLite重开、MA HTTP模拟、真实本地WebUI HTTP及页面脚本渲染；不是外部飞书/MA E2E。

性能：同一脚本对照9bd6ef0，200组交替SQLite+模拟传输；与测试并行的首次测量文本P95 0.469→0.539ms、双4.5MiB附件7.361→7.866ms，标注受并发测试干扰。测试/构建结束后独立重跑，文本P95 0.450→0.436ms、附件7.978→8.393ms（约+0.415ms），调用数一致、无额外核查。保留两次观测，不宣称真实用户性能提高。证据见docs/test-results/attachment-errors-2026-09-16.json。

未完成边界：飞书下载适配器的细分错误仍可能只显示unknown；新诊断不是PDF理解/提前idle修复，不是完整准备事务恢复或持久化队列默认启用。Memory/TOS/upgrade、部分写入受控续跑、其他回复恢复、真实E2E和回退继续按Stage 1–5原范围推进；测试资源授权未收到答复，未使用真实凭证、未部署或发布。

文件读取与回复观测增量（2026-09-16，本地基线8813933）：上一轮仅核对状态，无开发进展；本轮续接未提交改动，重新读取官方事件结构2559583（2026-09-09更新），确认agent.message包括中间说明，agent.tool_result表示内置工具执行完成。SSE与历史轮询共用有界读取观测器，按tool_use_id关联read/result，记录文档块、错误/缺失/未关联结果、回复相对读取的顺序。最多128次读取/1000个相关事件，冲突重复、乱序、多MA线程与截断降为待核实；不读Document.source、不存路径/正文/Base64，路径仅Hash，业务结果固定not_assessed。SQLite审计写入/读取白名单化，旧库加列，损坏诊断返回固定安全错误；WebUI展示事实和未自动验收说明，不回注入对话历史。

附件输入新增处理提示，要求收到文档结果后继续当前任务，不虚构下载等待或后台分析，不覆写SP。对可以明确观察到的“最后回复在read结果之前、之后直接idle”修复最终交付：提示未收到读取后的回复，失败审计保留观测，原Session不变且不自动重跑；正常读后回复、歧义事件、无文件会话保持原行为。客户实际“读后说稍后分析再idle”仍未复现，没有用关键词分类伪装关闭此问题。

验证：新增38项，完整686/686通过，零失败/取消/跳过（7226.4325ms）；check仅语法、build、diff检查通过。覆盖7字节分片SSE与轮询、旧事件/多轮隔离、重复冲突/乱序/上限、正文getter拒绝访问、旧库迁移/重开、损坏JSON脱敏、直接附件/未@历史/引用文件/普通文本输入、流式交付失败及非流式早停、真实本机HTTP和实际页面脚本；外部MA/飞书仍模拟。延迟门控测试确认两份文件下载/上传及创建Session挂载完成前不会调用MA.run，不能据此断言客户环境必然无下载问题。

性能：200组交替真实SQLite对照8813933。已解析的两份6MiB Document载荷元数据归并+审计写入，P50 0.077→0.104ms，P95 0.346→0.301ms；普通事件P95 0.321→0.262ms。正常Gateway双4.5MiB附件P95 10.020→9.831ms，普通文本0.627→0.617ms；两组download/upload/mount/run均410、额外inspect=0。样本存在噪声，不宣称真实用户速度提升；元数据测试不包含网络/SSE解析或PDF理解。新增脚本benchmark-file-observation.mjs，完整证据见docs/test-results/file-read-observation-2026-09-16.json。

边界：没有客户Session/原PDF复现证据；未知/缺失事件无法自动业务验收；授权等待返回分支的文件观测尚未单独落入此审计列；真实文件理解、完整准备事务/CLI队列默认启用、部分写入受控续跑、Memory/TOS/upgrade和全真实E2E继续待完成。没有收到新测试资源授权，没有访问被拒绝的私有Session，未部署或发布，也未改变durableQueue默认关闭。

Session升级协议层增量（2026-09-16，本地基线34ecfa5）：上一轮仅核对状态，无开发进展；本轮按MA技能完整读取参考，通过官方页面2673930（2026-09-10更新）确认POST /sessions/{sessionId}/upgrades及agent/environment/initial_events/vault_ids四字段，通过2555937（2026-09-11更新）确认查询详情的裸Session对象。升级页没有展开嵌套schema，未将Create接口的id/type/资源字段猜作升级契约。新增原生JSON请求校验与规范Hash、有界单次提交、严格响应ID/类型/时间校验、只读状态轮询；返回只保留状态和配置Hash，未知状态归一化，不输出配置原文。升级响应丢失不重发initial_events；只有明确400/InvalidParameter分类为拒绝，其他结果未知。只有本次观察upgrading→idle报告settled，明确configurationVerified=false/businessResult=not_assessed；超时/取消不取消远端升级，查询错误不忙重试，旧回执/倒退时间/换Session不作完成证据。README撤销“平台永远不能追加Vault”的过度断言，保留当前预挂Vault、更新Credential策略。

验证：初始26项全部因缺方法失败，实现后通过；追加5项发现未知状态原文泄露、回执字段未校验、调用方后改回执影响轮询等3项问题，修复后31/31通过。再加入3项真实127.0.0.1 HTTP传输测试：7字节分片UTF-8、服务端已接收initial_events但断开响应、轮询正文未完成时Abort关闭连接。最终新增34项，完整720/720通过、零失败/取消/跳过（6121.805375ms）；语法check、build、diff通过。HTTP服务端模拟MA，不是825真实升级或真实飞书端到端，未使用真实凭证/额度；正常请求路径未新增调用。证据见docs/test-results/session-upgrade-protocol-2026-09-16.json。

未完成：当前只是原生协议层，不是U01完成。网关升级入口、App/Agent/Vault身份策略、同Session业务互斥、持久化升级意图和重启恢复、目标配置/沙箱实际生效、动态MemoryStore仍待接通与验收；不可将settled作为这些完成的证明。嵌套升级配置契约及真实测试授权待补齐，其他队列/部分写入续办/PDF/Memory/TOS与Stage 5保持原范围，未部署、发布或默认启用实验队列。

文本分片回复恢复增量（2026-09-16，本地基线572401e）：上一轮仅汇总进度和复跑已有63项，无新增开发；本轮收尾前一轮未提交的文本恢复。沿飞书IM技能读取官方消息GET契约与Channel SDK 0.4.1源码，确认text正文JSON、应用/租户/群/话题身份字段、chunkIds包含首条ID且文本切分不额外加换行。发送前保存完整正文Hash，只有全部消息ID已持久化、原MA运行与回复意图一致时，按序GET全部分片；每片归属、类型、未撤回、完整正文Hash均匹配才原子保存证明并结束原任务。最多50条、单片正文1MiB、总正文4MiB、共5秒等待；SDK底层HTTP不声称已物理取消。旧无意图记录、Markdown转post、无ID/部分发送不能补造证明，CLI实验队列仍关闭。

验证：新增30项，最终完整750/750通过，零失败/取消/跳过（6568.919708ms），check（语法）、build和diff检查通过。真实Node子进程在保存全部回复ID后退出，单聊/群聊/话题重开Gateway+SQLite核查两片，0次重跑/重发、Session不变。缺片或跨话题时后续同scope消息保留；可读且证明完整后放行，仅执行后续请求。另覆盖SQLite落盘失败无半确认、CAS、证明ID顺序/模式/Hash/时效、原MA结果变化、加密重开、查询参数/作用域被修改、取消、50片串行和总字节上限。扩展测试修正JSON省略undefined字段的夹具比较；新增测试还发现发送中调用方改写文本导致最终指纹变化，已固定发送前指纹并复用。外部MA/飞书为模拟，不作真实E2E声明。证据见docs/test-results/text-reply-recovery-2026-09-16.json。

性能尚未通过：新增benchmark-text-reply-recovery.mjs，以git archive的572401e作对照，分别200/1000/1000组交替运行真实Gateway、SQLite与适配层。每组MA.run/send次数一致、创建1次Session、0额外核查。最终1000样本默认队列关闭：短文本P95 1.133→1.175ms，长文本1.553→2.695ms；实验队列开启：短文本2.905→3.076ms，长文本5.594→5.058ms。三次原始结果全部保留，尚不能将长文本尾延迟回退归因为GC、磁盘或具体代码；达到同基准三次尝试上限，不继续盲目重跑。下一步先拆分SQLite/调度/适配层计时，并评估固定交替顺序偏差，再决定改动。此增量可本地提交，但不部署/发布，不宣布性能或整个目标完成。

其余边界保持：无消息ID/部分分片、SDK流式回退、完整准备事务与CLI持久队列启用、部分写入续办、Session升级网关闭环、动态Memory/TOS、客户PDF问题、真实飞书/MA全部场景及回退演练仍未完成。开发机没有自动更新；测试资源授权与客户PDF材料仍需补齐。

## Stage 5: 完整回归与真实端到端
**Goal**: 自动化、真实MA、开发机真实飞书、性能对照、迁移与回退演练。
**Success Criteria**: 全部确定性测试通过；安全失败零容忍；真实用户入口证据，不以模拟替代；逐项完成审计。
**Tests**: 个人助手/数字员工单聊/群聊/Thread，排队表情/流式，OAuth/文件；同配置延迟对照。
**Status**: In Progress

文本恢复性能定位（2026-09-16，本地基线100430a）分三步继续：①补独立分段计时与均衡调度顺序测试，避免继续原基准盲跑；②以同版本A/A校准，再以572401e/当前代码A/B对照，保留数据库、适配层、结束后等待、各运行位置的分布；③有定位证据才改产品代码并完整回归。成功标准不是选一轮变快数据，而是解释或修复已观察差异且不增加正常远程调用。未达标继续保留性能验收未完成，不部署/发布、不改变原阶段范围。

定位结果：新增profile-text-reply-recovery.mjs，把成功终态落盘前、Store方法、扣除Store的回复适配层、落盘后等待分别计时，8轮均衡每个变体的执行位置；保留旧基准不改原结果。每组每种文本512样本/16预热。同代码A/A默认长文本落盘前P95=1.587/1.583ms，但旧口径总P95=2.340/1.854ms，证明总计时存在不能归因为代码版本的变化。A/B默认长文本落盘前P95=1.266→1.366ms，回复适配层0.007375→0.007666ms，总P95=2.043→1.779ms；实验队列长文本落盘前4.372→4.363ms、短文本3.213→3.535ms。最慢样本可定位到Store方法耗时或成功落盘后的调度等待，但不能据此进一步断言磁盘/GC/CPU原因。原先约1ms的总P95差异并未被证实为回复恢复代码回退；没有擅自减少事务/持久性，也没有宣称整个性能验收通过。

新增7项诊断工具测试（初始缺模块失败后实现），含计时嵌套不重复、失败不发完成信号、不污染其他实例、位置均衡、统计不修改原样本、跨Store/Inbox计时及真实Gateway命令小样本校验。全量757/757通过、零失败/取消/跳过（6818.649375ms）；语法check、脚本检查、build、diff通过。所有性能组各1次create、1056次run/send、0次inspect，默认模式observer为0；MA/飞书传输均模拟。两轮完整报告和最慢样本明细保存在docs/test-results/text-reply-profile-2026-09-16.json。本轮未改产品运行时代码，未部署/发布；完整准备事务、部分写入续办、upgrade网关、PDF复现、Memory/TOS及真实E2E/性能/回退等原范围继续未完成。

恢复开发（2026-09-16）：上一目标轮仅核对剩余项，没有开发进展；本轮重新读取工作树后开始附件管理员诊断入口，先补测试再接通应用隔离、只读分页与WebUI。保持Stage 1–5原范围，不将诊断入口代替准备事务恢复、PDF业务理解和完整真实验收。

开发机部署证据补充（用户显式请求）：def1f88 / 0.2.9已部署10.36.3.236。开发机Node 22.16完整553/553通过；停机完整备份、备份副本迁移及实际迁移通过，原配置和5条会话绑定未变化、旧授权1条加密、数据库quick_check=ok，WebUI/API=200、飞书ws client ready、NRestarts=0。尚未执行实际回退演练；不代表真实业务端到端通过。durableQueue仍关闭。后续开发不自动部署到用户正在测试的服务。

## 验收编号与证据登记

### 授权代次与准备恢复（2026-09-17，基线1070b63）

## Stage Q1: 稳定授权代次
**Goal**: Token刷新不改变授权代次；重新OAuth、权限集合或凭证绑定变化使旧准备任务失效。
**Success Criteria**: 代次随凭证加密保存，旧记录不伪造旧任务证明，CAS失败无半更新。
**Tests**: 刷新/同步、重新授权、scope重排、权限缩减、资源替换、重启与旧库兼容。
**Status**: Complete

## Stage Q2: 授权绑定的准备恢复
**Goal**: 准备计划和ready输入绑定完整用户身份/授权代次；使用明确的用户凭证维护接口安全恢复。
**Success Criteria**: 活跃OAuth、取消/新流程、未知刷新、缺证明和跨身份均不派发；已派发未知任务不重放；普通群聊保持Bot-only。
**Tests**: 实际子进程中断、原Session恢复、凭证过期、等待期间换授权、流式初始化和MA历史查询中的竞态。
**Status**: Complete

## Stage Q3: 完整回归与边界审计
**Goal**: 全量单元/进程测试、检查构建和性能调用数验证，明确与真实外部验收的差距。
**Success Criteria**: 不减少原Stage1–5范围，不自动部署或发布，不把mock OAuth当真实授权验收。
**Tests**: 完整测试集、check/build/diff、默认与持久化路径调用数。
**Status**: Complete

本增量结果：凭证密文保存稳定authorizationGeneration，普通Token刷新和同Credential同步不换代；重新OAuth、scope集合或Vault/Credential绑定变化、确认失效换代。准备步骤与ready输入保存Channel/App/租户/用户、原资源、generation和flowId证明；旧数据不现场补造。专用prepare/refresh/matches接口分开新准备与原绑定维护，重启后原Session/输入/凭证复用，未知刷新和未知hook仍暂停，不重新授权或预置。fresh确认失效并完成占位凭证同步后保留Bot能力，由实际工具权限错误触发OAuth；旧prepared任务不能采纳新授权。群聊和Thread保持Bot-only。

测试与审查补齐两个竞态：卡片初始化或MA历史/SSE等待期间Session/授权变化，在实际POST前同步拒绝；ready恢复不再先执行可新建Bot Credential的普通维护hook，专用refresh只维护已确认的原Credential。新输入和未接入生命周期的旧用法保持原行为。新增135项，最终完整1355/1355通过，零失败/取消/跳过（14479.973417ms），check（语法）、build、diff通过。包含真实Node子进程退出/SQLite重开、127.0.0.1 HTTP/SSE门禁测试、跨身份/重启/取消与刷新并发；外部MA/OAuth/飞书均模拟，不是825真实授权验收。独立审查无剩余阻塞项。证据见docs/test-results/authorization-preparation-2026-09-17.json。

性能：基线1070b63，128样本/16预热、默认与durable均衡交替，计时到成功终态落盘并独立SQLite复查。初测配对中位开销+0.623/+0.827ms，定位到重复领取凭证维护租约后合并为一次，最终+0.426/+0.655ms；正常每轮run/POST/send各1次、额外刷新/创建/核查0。保留初测、单租约优化后和最终门禁版本三轮，不将本地亚毫秒变化说成用户端性能优化或整体性能验收通过。

范围边界：CLI实验队列仍关闭，初始Vault/Credential未知预置、未知hook/缺失二进制缓存的人工处理、真实飞书/OAuth与回退演练、Session升级网关闭环、Memory/TOS、客户PDF复现及原Stage1–5其他工作继续未完成。发送前门禁不撤销已被MA接收的任务，也不保证运行中工具与外部凭证更新原子隔离。开发机保持稳定npm0.2.9，未部署或发布；本轮有具体开发与验证进展，不属于阻塞轮。

### 准备过程恢复增量（2026-09-17）

## Stage P1: 冻结准备步骤
**Goal**: 在实验持久队列中加密保存目标 Session、历史/引用、附件选择及步骤结果；旧任务不补造证据。
**Success Criteria**: revision/owner/planId CAS；已完成输出不可替换；任意 hook 结果未知时冻结。
**Tests**: 密文重开、旧版本兼容、错误绑定、过期回调、FIFO、日志大小边界。
**Status**: Complete

## Stage P2: 原任务恢复
**Goal**: 重启后仅继续原任务未完成准备，复用已确认上传/挂载/创建，不重取已冻结历史。
**Success Criteria**: 原 inboxId、原文件范围、原预算；未知写只读核查；ready 后沿已有派发逻辑。
**Tests**: 实际子进程在历史、hook、上传、部分历史附件、ready 前退出；父进程不发送新消息。
**Status**: Complete

## Stage P3: 回归与证据
**Goal**: 验证默认路径无额外调用，完整本地回归并记录未覆盖的真实场景。
**Success Criteria**: 全测试、check、build、diff 通过；开发机保持稳定版，不默认启用实验队列。
**Tests**: 全量测试与正常路径调用数/耗时对照。
**Status**: Complete

本增量结果：v3加密准备计划保存原目标Session、历史/引用观察、历史筛选和附件入选、逐项预算/结果、hook输出与最终创建请求；ready后删除中间计划。每步owner/revision/planId CAS、256步/单输出2MiB/计划8MiB限制，旧记录只兼容读取、不补造证据。completed跳过操作，pending上传/挂载/创建仅凭原journal恢复；pending观察/任意hook/本地选择结果未保存则冻结。下载无缓存且无upload意图暂停，不以Hash代替字节。未执行自定义hook缺少revision不重入，partial dualIdentity单聊尚无授权代次证明，保守暂停；ready恢复的每轮凭证维护不变。

测试发现并修复三处边界：附件提示重复脱敏丢失超限说明；领取后新授权暂停仍可能派发；创建完成但映射被清除后仍向旧Session准备文件。新增95项，最终完整1220/1220通过、零失败/取消/跳过（10947.635083ms），check（语法）、build、diff检查通过。真实子进程仅恢复原Inbox，不用新消息替代：历史/引用保持旧快照、9文件只沿用原8文件选择、40MiB与256KiB预算不漂移、unknown上传仅GET核查、无下载缓存暂停、hook未知/配置/Session变更拒绝。WebUI原reconcile仍只允许已派发核查，未隐式扩大管理按钮权限。

真实MA：scripts/probe-prepared-recovery.mjs新增--preparing（旧ready保持），创建1个无Vault Session后在session-create步骤确认、ready尚不存在时exit77。Session sesn-20260916165406-vl6sh，child创建1/模型0；parent核查1/模型POST1，创建、历史、资源hook、上传均0，原计划/input Hash匹配，nonce回复与原生事件一致。总9408ms（含子进程），0工具/0compact命令/0compact事件/0Session错误；未连接飞书。证据在docs/test-results/preparation-plan-2026-09-17.json，不把nonce回显当作真实飞书文件/OAuth完整验收。

性能：默认路径200组普通文本与双4.5MiB附件，首次与全量并行的结果保留；独立复跑文本P95 0.429→0.437ms、文件7.675→8.797ms，文件P50 6.239→6.247ms，尾部差异尚未定位。均衡分段128组，durable终态落盘配对中位增加约1.3ms，主要在Store方法（加密/序列化/落盘合计）。所有组调用数一致，额外inspect=0。没有通过减少持久性来掩盖开销，也不宣称整体性能验收完成。

继续保持Stage1–5完整范围：缺失下载内容/未知回调人工处理、稳定授权代次与维护hook安全重入、CLI实验队列启用、真实飞书卡片/OAuth、Session升级网关闭环、Memory/TOS组合、客户原PDF、受控续办与回退演练尚未完成。开发机保持稳定npm0.2.9，未部署/发布；本轮有开发与真实验证进展，不属于阻塞轮。

- C01-C06：引用理解、窗口外定向获取、撤回/权限、Thread、注入防护、多人身份（待验证）。
- S01-S05：无配置兼容、Memory/TOS挂载、合并资源、身份冲突拒绝、配置变化不重建（待验证）。
- P01-P05：同样本防重、重启、无消息、串行压缩、失败不轮换（待验证）。
- A01-A07：旧Session刷新、失效OAuth、错误分类、多应用隔离、Credential同步、回调、写入不重放（待验证）。
- F01-F05：未@附件、部分失败、提前idle、文件持久、SSE（待验证）。
- E01-E02：AppID与脱敏版本诊断（待验证）。
- U01：upgrade顶层协议、单次提交和有界状态轮询已完成本地测试；嵌套契约、网关身份边界/互斥/持久化恢复及实际生效待完成。
- 平台依赖：MA官方页面此前读取失败；本轮重新寻找可验证契约。测试额度使用已有授权账号，生产Agent不自动修改。
- 发布：本次开发目标不等于授权推送npm/GitHub或切换生产网关；完成后提供可验收代码与证据，外部发布按明确请求执行。
